{
  "name": "fb-lead-finder-dashboard",
  "version": "1.0.0",
  "description": "Dashboard for FB Lead Finder pipeline",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "rebuild": "npx @electron/rebuild -f -w better-sqlite3",
    "setup": "npm install && npx @electron/rebuild -f -w better-sqlite3"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "electron": "^35.0.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.0"
  }
}
