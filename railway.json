{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "cd pipeline && node index.js",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 3
  }
}
